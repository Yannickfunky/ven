{% extends 'base.html.twig' %}

{% block title %}Hello CreateUtilisateurController!{% endblock %}

{% block container %}
    <h1>Inscription</h1>

    {{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password) }}
    <button type="submit">Créer l'utilisateur</button>
    {{ form_end(form) }}
{% endblock %}

